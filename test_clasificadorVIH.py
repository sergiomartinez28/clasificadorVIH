import clasificadorVIH

def test0():
    text = clasificadorVIH.get_text_from_file('datasets', 0)
    assert clasificadorVIH.detect_sida(text) == False
    
def test1():
    text = clasificadorVIH.get_text_from_file('datasets', 1)
    assert clasificadorVIH.detect_sida(text) == False
        
def test2():
    text = clasificadorVIH.get_text_from_file('datasets', 2)
    assert clasificadorVIH.detect_sida(text) == False
    
    
def test3():
    text = clasificadorVIH.get_text_from_file('datasets', 3)
    assert clasificadorVIH.detect_sida(text) == False
    
    
def test4():
    text = clasificadorVIH.get_text_from_file('datasets', 4)
    assert clasificadorVIH.detect_sida(text) == False
    
def test5():
    text = clasificadorVIH.get_text_from_file('datasets', 5)
    assert clasificadorVIH.detect_sida(text) == False
        
def test6():
    text = clasificadorVIH.get_text_from_file('datasets', 6)
    assert clasificadorVIH.detect_sida(text) == False       

def test7():
    text = clasificadorVIH.get_text_from_file('datasets', 7)
    assert clasificadorVIH.detect_sida(text) == False      

def test8():
    text = clasificadorVIH.get_text_from_file('datasets', 8)
    assert clasificadorVIH.detect_sida(text) == False      

def test9():
    text = clasificadorVIH.get_text_from_file('datasets', 9)
    assert clasificadorVIH.detect_sida(text) == False      
 
def test10():
    text = clasificadorVIH.get_text_from_file('datasets', 10)
    assert clasificadorVIH.detect_sida(text) == False  

def test11():
    text = clasificadorVIH.get_text_from_file('datasets', 11)
    assert clasificadorVIH.detect_sida(text) == False  

def test12():
    text = clasificadorVIH.get_text_from_file('datasets', 12)
    assert clasificadorVIH.detect_sida(text) == False

def test13():
    text = clasificadorVIH.get_text_from_file('datasets', 13)
    assert clasificadorVIH.detect_sida(text) == False

def test14():
    text = clasificadorVIH.get_text_from_file('datasets', 14)
    assert clasificadorVIH.detect_sida(text) == False
    
def test15():
    text = clasificadorVIH.get_text_from_file('datasets', 15)
    assert clasificadorVIH.detect_sida(text) == False   

def test16():
    text = clasificadorVIH.get_text_from_file('datasets', 16)
    assert clasificadorVIH.detect_sida(text) == False
    
def test17():
    text = clasificadorVIH.get_text_from_file('datasets', 17)
    assert clasificadorVIH.detect_sida(text) == False
    
def test18():
    text = clasificadorVIH.get_text_from_file('datasets', 18)
    assert clasificadorVIH.detect_sida(text) == False

def test19():
    text = clasificadorVIH.get_text_from_file('datasets', 19)
    assert clasificadorVIH.detect_sida(text) == False

def test20():
    text = clasificadorVIH.get_text_from_file('datasets', 20)
    assert clasificadorVIH.detect_sida(text) == False
    
def test21():
    text = clasificadorVIH.get_text_from_file('datasets', 21)
    assert clasificadorVIH.detect_sida(text) == False
        
def test22():
    text = clasificadorVIH.get_text_from_file('datasets', 22)
    assert clasificadorVIH.detect_sida(text) == False
    
    
def test23():
    text = clasificadorVIH.get_text_from_file('datasets', 23)
    assert clasificadorVIH.detect_sida(text) == False
    
    
def test24():
    text = clasificadorVIH.get_text_from_file('datasets', 24)
    assert clasificadorVIH.detect_sida(text) == False
    
def test25():
    text = clasificadorVIH.get_text_from_file('datasets', 25)
    assert clasificadorVIH.detect_sida(text) == False
        
def test26():
    text = clasificadorVIH.get_text_from_file('datasets', 26)
    assert clasificadorVIH.detect_sida(text) == False       

def test27():
    text = clasificadorVIH.get_text_from_file('datasets', 27)
    assert clasificadorVIH.detect_sida(text) == False      

def test28():
    text = clasificadorVIH.get_text_from_file('datasets', 28)
    assert clasificadorVIH.detect_sida(text) == False      

def test29():
    text = clasificadorVIH.get_text_from_file('datasets', 29)
    assert clasificadorVIH.detect_sida(text) == False      
 
def test30():
    text = clasificadorVIH.get_text_from_file('datasets', 30)
    assert clasificadorVIH.detect_sida(text) == False  

def test31():
    text = clasificadorVIH.get_text_from_file('datasets', 31)
    assert clasificadorVIH.detect_sida(text) == False  

def test32():
    text = clasificadorVIH.get_text_from_file('datasets', 32)
    assert clasificadorVIH.detect_sida(text) == False

def test33():
    text = clasificadorVIH.get_text_from_file('datasets', 33)
    assert clasificadorVIH.detect_sida(text) == False

def test34():
    text = clasificadorVIH.get_text_from_file('datasets', 34)
    assert clasificadorVIH.detect_sida(text) == False
    
def test35():
    text = clasificadorVIH.get_text_from_file('datasets', 35)
    assert clasificadorVIH.detect_sida(text) == False   

def test36():
    text = clasificadorVIH.get_text_from_file('datasets', 36)
    assert clasificadorVIH.detect_sida(text) == False
    
def test37():
    text = clasificadorVIH.get_text_from_file('datasets', 37)
    assert clasificadorVIH.detect_sida(text) == False
    
def test38():
    text = clasificadorVIH.get_text_from_file('datasets', 38)
    assert clasificadorVIH.detect_sida(text) == False

def test39():
    text = clasificadorVIH.get_text_from_file('datasets', 39)
    assert clasificadorVIH.detect_sida(text) == False

def test40():
    text = clasificadorVIH.get_text_from_file('datasets', 40)
    assert clasificadorVIH.detect_sida(text) == False  

def test41():
    text = clasificadorVIH.get_text_from_file('datasets', 41)
    assert clasificadorVIH.detect_sida(text) == False  

def test42():
    text = clasificadorVIH.get_text_from_file('datasets', 42)
    assert clasificadorVIH.detect_sida(text) == False

def test43():
    text = clasificadorVIH.get_text_from_file('datasets', 43)
    assert clasificadorVIH.detect_sida(text) == False

def test44():
    text = clasificadorVIH.get_text_from_file('datasets', 44)
    assert clasificadorVIH.detect_sida(text) == False
    
def test45():
    text = clasificadorVIH.get_text_from_file('datasets', 45)
    assert clasificadorVIH.detect_sida(text) == False   

def test46():
    text = clasificadorVIH.get_text_from_file('datasets', 46)
    assert clasificadorVIH.detect_sida(text) == False
    
def test47():
    text = clasificadorVIH.get_text_from_file('datasets', 47)
    assert clasificadorVIH.detect_sida(text) == False
    
def test48():
    text = clasificadorVIH.get_text_from_file('datasets', 48)
    assert clasificadorVIH.detect_sida(text) == False

def test49():
    text = clasificadorVIH.get_text_from_file('datasets', 49)
    assert clasificadorVIH.detect_sida(text) == False

def test50():
    text = clasificadorVIH.get_text_from_file('datasets', 50)
    assert clasificadorVIH.detect_sida(text) == True
    
def test51():
    text = clasificadorVIH.get_text_from_file('datasets', 51)
    assert clasificadorVIH.detect_sida(text) == True
        
def test52():
    text = clasificadorVIH.get_text_from_file('datasets', 52)
    assert clasificadorVIH.detect_sida(text) == True
    
    
def test53():
    text = clasificadorVIH.get_text_from_file('datasets', 53)
    assert clasificadorVIH.detect_sida(text) == True
    
    
def test54():
    text = clasificadorVIH.get_text_from_file('datasets', 54)
    assert clasificadorVIH.detect_sida(text) == True
    
def test55():
    text = clasificadorVIH.get_text_from_file('datasets', 55)
    assert clasificadorVIH.detect_sida(text) == True
        
def test56():
    text = clasificadorVIH.get_text_from_file('datasets', 56)
    assert clasificadorVIH.detect_sida(text) == True       

def test57():
    text = clasificadorVIH.get_text_from_file('datasets', 57)
    assert clasificadorVIH.detect_sida(text) == True      

def test58():
    text = clasificadorVIH.get_text_from_file('datasets', 58)
    assert clasificadorVIH.detect_sida(text) == True      

def test59():
    text = clasificadorVIH.get_text_from_file('datasets', 59)
    assert clasificadorVIH.detect_sida(text) == True      
 
def test60():
    text = clasificadorVIH.get_text_from_file('datasets', 60)
    assert clasificadorVIH.detect_sida(text) == True  
    
def test61():
    text = clasificadorVIH.get_text_from_file('datasets', 61)
    assert clasificadorVIH.detect_sida(text) == True
        
def test62():
    text = clasificadorVIH.get_text_from_file('datasets', 62)
    assert clasificadorVIH.detect_sida(text) == True
    
    
def test63():
    text = clasificadorVIH.get_text_from_file('datasets', 63)
    assert clasificadorVIH.detect_sida(text) == True
    
    
def test64():
    text = clasificadorVIH.get_text_from_file('datasets', 64)
    assert clasificadorVIH.detect_sida(text) == True
    
def test65():
    text = clasificadorVIH.get_text_from_file('datasets', 65)
    assert clasificadorVIH.detect_sida(text) == True
        
def test66():
    text = clasificadorVIH.get_text_from_file('datasets', 66)
    assert clasificadorVIH.detect_sida(text) == True       

def test67():
    text = clasificadorVIH.get_text_from_file('datasets', 67)
    assert clasificadorVIH.detect_sida(text) == True      

def test68():
    text = clasificadorVIH.get_text_from_file('datasets', 68)
    assert clasificadorVIH.detect_sida(text) == True      

def test69():
    text = clasificadorVIH.get_text_from_file('datasets', 69)
    assert clasificadorVIH.detect_sida(text) == True      
 
def test70():
    text = clasificadorVIH.get_text_from_file('datasets', 70)
    assert clasificadorVIH.detect_sida(text) == True
    
def test71():
    text = clasificadorVIH.get_text_from_file('datasets', 71)
    assert clasificadorVIH.detect_sida(text) == True
        
def test72():
    text = clasificadorVIH.get_text_from_file('datasets', 72)
    assert clasificadorVIH.detect_sida(text) == True
    
def test73():
    text = clasificadorVIH.get_text_from_file('datasets', 73)
    assert clasificadorVIH.detect_sida(text) == True
    
def test74():
    text = clasificadorVIH.get_text_from_file('datasets', 74)
    assert clasificadorVIH.detect_sida(text) == True
    
def test75():
    text = clasificadorVIH.get_text_from_file('datasets', 75)
    assert clasificadorVIH.detect_sida(text) == True
        
def test76():
    text = clasificadorVIH.get_text_from_file('datasets', 76)
    assert clasificadorVIH.detect_sida(text) == True       

def test77():
    text = clasificadorVIH.get_text_from_file('datasets', 77)
    assert clasificadorVIH.detect_sida(text) == True      

def test78():
    text = clasificadorVIH.get_text_from_file('datasets', 78)
    assert clasificadorVIH.detect_sida(text) == True      

def test79():
    text = clasificadorVIH.get_text_from_file('datasets', 79)
    assert clasificadorVIH.detect_sida(text) == True      

def test80():
    text = clasificadorVIH.get_text_from_file('datasets', 80)
    assert clasificadorVIH.detect_sida(text) == True
    
def test81():
    text = clasificadorVIH.get_text_from_file('datasets', 81)
    assert clasificadorVIH.detect_sida(text) == True
        
def test82():
    text = clasificadorVIH.get_text_from_file('datasets', 82)
    assert clasificadorVIH.detect_sida(text) == True
    
def test83():
    text = clasificadorVIH.get_text_from_file('datasets', 83)
    assert clasificadorVIH.detect_sida(text) == True
    
def test84():
    text = clasificadorVIH.get_text_from_file('datasets', 84)
    assert clasificadorVIH.detect_sida(text) == True
    
def test85():
    text = clasificadorVIH.get_text_from_file('datasets', 85)
    assert clasificadorVIH.detect_sida(text) == True
        
def test86():
    text = clasificadorVIH.get_text_from_file('datasets', 86)
    assert clasificadorVIH.detect_sida(text) == True       

def test87():
    text = clasificadorVIH.get_text_from_file('datasets', 87)
    assert clasificadorVIH.detect_sida(text) == True      

def test88():
    text = clasificadorVIH.get_text_from_file('datasets', 88)
    assert clasificadorVIH.detect_sida(text) == True      

def test89():
    text = clasificadorVIH.get_text_from_file('datasets', 89)
    assert clasificadorVIH.detect_sida(text) == True      

def test90():
    text = clasificadorVIH.get_text_from_file('datasets', 90)
    assert clasificadorVIH.detect_sida(text) == True
    
def test91():
    text = clasificadorVIH.get_text_from_file('datasets', 91)
    assert clasificadorVIH.detect_sida(text) == True
        
def test92():
    text = clasificadorVIH.get_text_from_file('datasets', 92)
    assert clasificadorVIH.detect_sida(text) == True
    
    
def test93():
    text = clasificadorVIH.get_text_from_file('datasets', 93)
    assert clasificadorVIH.detect_sida(text) == True
    
    
def test94():
    text = clasificadorVIH.get_text_from_file('datasets', 94)
    assert clasificadorVIH.detect_sida(text) == True
    
def test95():
    text = clasificadorVIH.get_text_from_file('datasets', 95)
    assert clasificadorVIH.detect_sida(text) == True
        
def test96():
    text = clasificadorVIH.get_text_from_file('datasets', 96)
    assert clasificadorVIH.detect_sida(text) == True       

def test97():
    text = clasificadorVIH.get_text_from_file('datasets', 97)
    assert clasificadorVIH.detect_sida(text) == True      

def test98():
    text = clasificadorVIH.get_text_from_file('datasets', 98)
    assert clasificadorVIH.detect_sida(text) == True      

def test99():
    text = clasificadorVIH.get_text_from_file('datasets', 99)
    assert clasificadorVIH.detect_sida(text) == True      
